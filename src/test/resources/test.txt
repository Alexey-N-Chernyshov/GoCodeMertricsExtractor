package main

import "fmt"

func (p *Point) Length() float64 {
//	return math.Sqrt(p.x * p.x + p.y * p.y)
}

func main() {
    var a string = "initial"
    fmt.Println(a)

    var b, c int = 1, 2
    fmt.Println(b, c)

    var d = true
    fmt.Println(d)

    var e int
    fmt.Println(e)

//    f := "short"
    fmt.Println(f)
}